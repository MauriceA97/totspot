import { Typography } from '@mui/material';

const Home = () => {
  return (
    <div>
      <Typography variant="h4">Welcome to TotSpot</Typography>
      <Typography variant="body1">Find the perfect pediatric care provider for your child</Typography>
    </div>
  );
};

export default Home;